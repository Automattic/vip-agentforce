# AGENTS: VIP Agentforce (Public Repo)

This repository is public. Keep all changes safe for public visibility.

## Public Safety Rules

- Do not mention private repository URLs or internal hosts.
- Do not add internal credentials, secrets, or implementation-only details.
- Keep backend references interface-based and public-safe.
- Before commit, run:

```bash
../../bin/check-public-guardrails.sh --repo "$(pwd)" --all
```

## Collaboration Context

- This plugin is one part of a larger integration that also includes private backend/dashboard/Salesforce repos.
- Treat private-side behavior as contracts, not as public implementation detail.
